# URL absoluta da API. Em dev local, o padrao e VAZIO (proxy Vite /v1 -> :3333).
#
# | Cenario              | VITE_API_BASE_URL              |
# |----------------------|--------------------------------|
# | Docker dev-up :3333  | (vazio) — ver .env.local.docker.example |
# | API host Inter QA    | http://localhost:3334 — ver .env.local.inter-homolog.example |
# | Producao / tunel     | https://sua-api.exemplo.com    |
#
# VITE_API_BASE_URL=
